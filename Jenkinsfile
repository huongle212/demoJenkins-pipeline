pipeline {
    agent any 
    
    stages {
        stage('Clone') { 
            steps {
                git clone 'https://github.com/huongle212/demoJenkins-pipeline.git'
            }
        }
    }
}
